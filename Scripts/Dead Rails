local Luna = loadstring(game:HttpGet("https://raw.githubusercontent.com/Nebula-Softworks/Luna-Interface-Suite/refs/heads/main/source.lua", true))()

local Window = Luna:CreateWindow({
	Name = "Synergy Hub",
	Subtitle = "by v4mp7n",
	LogoID = nil,
	LoadingEnabled = true,
	LoadingTitle = "Synergy Hub",
	LoadingSubtitle = "powered by Luna UI",
	ConfigSettings = {
		ConfigFolder = "SynergyHub"
	},
	KeySystem = false
})

-- Home Tab
Window:CreateHomeTab({
	SupportedExecutors = {"Fluxus", "Trigon", "KRNL", "Electron", "Script-Ware"},
	DiscordInvite = "v4mp7n",
	Icon = 1
})

-- GUIS Tab
local guisTab = Window:CreateTab({
	Name = "Guis",
	Icon = "dashboard",
	ImageSource = "Material",
	ShowTitle = true
})

guisTab:CreateButton({ Name = "Dusty", Callback = function()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/HeadHarse/Dusty/refs/heads/main/Protected_6376837736278867.txt"))()
end })

guisTab:CreateButton({ Name = "Nullfire", Callback = function()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/InfernusScripts/Null-Fire/main/Loader"))()
end })

guisTab:CreateButton({ Name = "Ringta Scripts", Callback = function()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/ringtaa/deadrailsui.github.io/refs/heads/main/RINGTA.LUA"))()
end })

-- Teleports Tab
local tpTab = Window:CreateTab({
	Name = "Teleports",
	Icon = "location_on",
	ImageSource = "Material",
	ShowTitle = true
})

tpTab:CreateLabel({ Text = "If the TP doesn't work, wait 2 minutes.", Style = 1 })

tpTab:CreateButton({ Name = "Bank", Callback = function()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/ringtaa/Tptobank.github.io/refs/heads/main/Banktp.lua"))()
end })

tpTab:CreateButton({ Name = "Castle", Callback = function()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/ringtaa/castletpfast.github.io/refs/heads/main/FASTCASTLE.lua"))()
end })

tpTab:CreateButton({ Name = "Constitution", Callback = function()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/ringtaa/Tpfort.github.io/refs/heads/main/Tpfort.lua"))()
end })

tpTab:CreateButton({ Name = "End", Callback = function()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/ringtaa/newpacifisct/refs/heads/main/newpacifisct.lua"))()
end })

tpTab:CreateButton({ Name = "Farm", Callback = function()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/ringtaa/tpfarm.github.io/refs/heads/main/tptofarm.lua"))()
end })

tpTab:CreateButton({ Name = "Golden Eggs", Callback = function()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/ringtaa/deadrailsui.github.io/refs/heads/main/RINGTA.LUA"))()
end })

tpTab:CreateButton({ Name = "Sterling Town", Callback = function()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/ringtaa/sterlingnotifcation.github.io/refs/heads/main/Sterling.lua"))()
end })

tpTab:CreateButton({ Name = "Tesla", Callback = function()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/ringtaa/tptotesla.github.io/refs/heads/main/Tptotesla.lua"))()
end })

tpTab:CreateButton({ Name = "Train", Callback = function()
	loadstring(game:HttpGet("https://raw.githubusercontent.com/ringtaa/train.github.io/refs/heads/main/train.lua"))()
end })

-- Extra Tab
local extraTab = Window:CreateTab({
	Name = "Extra",
	Icon = "extension",
	ImageSource = "Material",
	ShowTitle = true
})

extraTab:CreateToggle({
	Name = "FullBright",
	Default = false,
	Callback = function(value)
		_G.FullBrightEnabled = value
		if not _G.FullBrightExecuted then
			_G.NormalLightingSettings = {
				Brightness = game.Lighting.Brightness,
				ClockTime = game.Lighting.ClockTime,
				FogEnd = game.Lighting.FogEnd,
				GlobalShadows = game.Lighting.GlobalShadows,
				Ambient = game.Lighting.Ambient
			}

			game.Lighting:GetPropertyChangedSignal("Brightness"):Connect(function()
				if _G.FullBrightEnabled then game.Lighting.Brightness = 1 end
			end)

			game.Lighting:GetPropertyChangedSignal("ClockTime"):Connect(function()
				if _G.FullBrightEnabled then game.Lighting.ClockTime = 12 end
			end)

			game.Lighting:GetPropertyChangedSignal("FogEnd"):Connect(function()
				if _G.FullBrightEnabled then game.Lighting.FogEnd = 786543 end
			end)

			game.Lighting:GetPropertyChangedSignal("GlobalShadows"):Connect(function()
				if _G.FullBrightEnabled then game.Lighting.GlobalShadows = false end
			end)

			game.Lighting:GetPropertyChangedSignal("Ambient"):Connect(function()
				if _G.FullBrightEnabled then game.Lighting.Ambient = Color3.fromRGB(178, 178, 178) end
			end)

			spawn(function()
				local lastState = _G.FullBrightEnabled
				while true do
					wait()
					if _G.FullBrightEnabled ~= lastState then
						if _G.FullBrightEnabled then
							game.Lighting.Brightness = 1
							game.Lighting.ClockTime = 12
							game.Lighting.FogEnd = 786543
							game.Lighting.GlobalShadows = false
							game.Lighting.Ambient = Color3.fromRGB(178, 178, 178)
						else
							game.Lighting.Brightness = _G.NormalLightingSettings.Brightness
							game.Lighting.ClockTime = _G.NormalLightingSettings.ClockTime
							game.Lighting.FogEnd = _G.NormalLightingSettings.FogEnd
							game.Lighting.GlobalShadows = _G.NormalLightingSettings.GlobalShadows
							game.Lighting.Ambient = _G.NormalLightingSettings.Ambient
						end
						lastState = _G.FullBrightEnabled
					end
				end
			end)

			_G.FullBrightExecuted = true
		end
	end
})

-- Credits Tab (Always at the end)
local creditsTab = Window:CreateTab({
	Name = "Credits",
	Icon = "info",
	ImageSource = "Material",
	ShowTitle = true
})

creditsTab:CreateLabel({ Text = "Discord: v4mp7n", Style = 2 })
creditsTab:CreateLabel({ Text = "Ringta Scripts: discord.gg/ringta", Style = 2 })

-- Load Autoload Config
Luna:LoadAutoloadConfig()
